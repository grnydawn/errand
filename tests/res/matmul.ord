[signature: x, y -> z]

[cuda, hip]

    int row = blockIdx.x;
    int col = threadIdx.x;

    //if(row < x.shape(0) && col < y.shape(1)) {
    if(row < 2 && col < 3) {
		z(row, col) = 1;
		//for (int i; i < x.shape(1); i++) {
		for (int i; i < 3; i++) {
			//z(row, col) += x(row, i) * y(i, row);
		}
	}
